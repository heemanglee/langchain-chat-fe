# Frontend PRD: RAG.OS 프론트엔드

## 1. 개요

RAG.OS는 LangChain 기반 RAG 서비스의 프론트엔드 애플리케이션이다. 문서 업로드, AI 에이전트 채팅, 폴더 기반 세션 관리, 세션 공유 기능을 제공하며, 라이트/다크 모드를 지원한다.

## 2. 기술 스택

| 카테고리 | 기술 | 비고 |
|----------|------|------|
| Framework | React 19 + Vite | SPA |
| Language | TypeScript 5.x | strict mode |
| Styling | Tailwind CSS 4.x | 라이트/다크 모드 |
| UI Components | shadcn/ui | Radix UI 기반 |
| State Management | Zustand | 경량 전역 상태 |
| Server State | TanStack Query (React Query) v5 | 캐싱, 무한스크롤 |
| Routing | React Router v7 | 중첩 라우트 |
| Form | React Hook Form + Zod | 유효성 검증 |
| HTTP Client | ky | fetch 래퍼 |
| Icons | Iconify (@iconify/react) | Solar 아이콘셋 |
| Markdown | react-markdown + remark-gfm | 채팅 응답 렌더링 |
| Code Highlight | Shiki | 코드 블록 하이라이트 |
| Testing | Vitest + Testing Library | 단위/통합 테스트 |
| E2E Testing | Playwright | E2E 테스트 |

## 3. 백엔드 API 기본 정보

- **Base URL:** `http://localhost:8004` (개발), 환경변수로 설정
- **인증:** Bearer Token (JWT)
- **응답 형식:** `{ status: number, message: string, data: T | null }`
- **스트리밍:** SSE (Server-Sent Events) - `POST /api/v1/chat/stream`

### 현재 구현된 API

| Method | Endpoint | 설명 |
|--------|----------|------|
| POST | `/api/auth/register` | 회원가입 |
| POST | `/api/auth/login` | 로그인 (JWT 발급) |
| POST | `/api/auth/logout` | 로그아웃 |
| POST | `/api/auth/refresh` | 토큰 갱신 |
| POST | `/api/v1/chat` | 동기 채팅 |
| POST | `/api/v1/chat/stream` | SSE 스트리밍 채팅 |
| GET | `/api/v1/conversations` | 대화 목록 (커서 페이지네이션) |

### 향후 추가 예정 API (PRD 기준)

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/api/v1/documents` | 문서 목록 |
| POST | `/api/v1/documents` | 문서 업로드 |
| DELETE | `/api/v1/documents/{id}` | 문서 삭제 |
| GET | `/api/v1/folders` | 폴더 목록 |
| POST | `/api/v1/folders` | 폴더 생성 |
| PATCH | `/api/v1/folders/{id}` | 폴더 수정 |
| DELETE | `/api/v1/folders/{id}` | 폴더 삭제 |
| POST | `/api/v1/sessions` | 세션 생성 |
| GET | `/api/v1/sessions` | 세션 목록 |
| GET | `/api/v1/sessions/{id}` | 세션 상세 |
| DELETE | `/api/v1/sessions/{id}` | 세션 삭제 |
| POST | `/api/v1/sessions/{id}/share` | 공유 링크 생성 |
| DELETE | `/api/v1/sessions/{id}/share` | 공유 취소 |
| GET | `/api/v1/sessions/shared/{token}` | 공유 세션 조회 (Public) |
| POST | `/api/v1/sessions/{id}/chat` | 세션 내 스트리밍 채팅 |

## 4. 라우트 구조

```
/                          → 랜딩 페이지 (비로그인)
/login                     → 로그인
/register                  → 회원가입
/chat                      → 메인 앱 (새 채팅)
/chat/:conversationId      → 특정 대화
/share/:shareToken         → 공유 세션 (Public, 읽기 전용)
```

## 5. 레이아웃 구조

### 5.1 메인 앱 레이아웃 (로그인 상태)

```
┌──────────────────────────────────────────────────────┐
│  Navbar (로고, 검색, 사용자 메뉴)                       │
├──────────┬───────────────────────────────────────────┤
│          │                                           │
│ Sidebar  │            Main Content                   │
│          │                                           │
│ LIBRARY  │   ┌───────────────────────────────────┐   │
│ ├ 모든문서│   │  Chat / Document List / etc.      │   │
│ ├ 최근업로드│   │                                   │   │
│          │   │                                   │   │
│ AGENTS   │   │                                   │   │
│ ├ 에이전트1│   │                                   │   │
│ ├ 에이전트2│   └───────────────────────────────────┘   │
│          │                                           │
│ SESSIONS │   ┌───────────────────────────────────┐   │
│ ├ 세션1   │   │  Input Area (메시지 입력)           │   │
│ ├ 세션2   │   └───────────────────────────────────┘   │
│          │                                           │
├──────────┴───────────────────────────────────────────┤
```

### 5.2 사이드바 구조

```
LIBRARY (문서 라이브러리)
├── 📁 모든 문서         ← 전체 문서 목록
├── 🕐 최근 업로드       ← 최근 업로드 문서

AGENTS (에이전트)
├── 👤 마케팅 분석 봇    ← 문서 기반 자동 생성 에이전트
├── 📄 계약서 검토 봇    ← 사용자 생성 에이전트

FOLDERS (폴더)
├── 📂 재무 리포트       ← 사용자 생성 폴더
├── 📂 기술 문서         ← 폴더별 세션 그룹

RECENT SESSIONS (최근 대화)
├── 💬 Q4 실적 분석      ← last_message_at 내림차순
├── 💬 마케팅 전략 검토
```

### 5.3 공유 뷰 레이아웃 (비로그인)

```
┌──────────────────────────────────────────────────────┐
│  Header (로고, 세션 제목, "RAG.OS 시작하기" 버튼)       │
├──────────────────────────────────────────────────────┤
│                                                      │
│             Chat Messages (읽기 전용)                  │
│                                                      │
│   ┌──────────────────────────────────────────────┐   │
│   │ User: 질문 내용                                │   │
│   └──────────────────────────────────────────────┘   │
│   ┌──────────────────────────────────────────────┐   │
│   │ Assistant: 답변 + Citation                    │   │
│   └──────────────────────────────────────────────┘   │
│                                                      │
├──────────────────────────────────────────────────────┤
│  "읽기 전용 모드입니다" (비활성 입력창)                   │
└──────────────────────────────────────────────────────┘
```

## 6. 개발 단계별 로드맵

| Phase | 내용 | 문서 |
|-------|------|------|
| **Phase 1** | 프로젝트 초기 설정 | [phase-1-project-setup.md](./phase-1-project-setup.md) |
| **Phase 2** | 인증 (로그인/회원가입/토큰관리) | [phase-2-auth.md](./phase-2-auth.md) |
| **Phase 3** | 레이아웃 + 채팅 (SSE 스트리밍) | [phase-3-layout-chat.md](./phase-3-layout-chat.md) |
| **Phase 4** | 문서 관리 + 폴더 관리 | [phase-4-documents-folders.md](./phase-4-documents-folders.md) |
| **Phase 5** | 세션 공유 + 랜딩 페이지 | [phase-5-sharing-landing.md](./phase-5-sharing-landing.md) |
| **Phase 6** | 다크 모드 + UX 폴리싱 | [phase-6-dark-mode-polish.md](./phase-6-dark-mode-polish.md) |
